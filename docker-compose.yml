services:
  command-bot:
    container_name: command-bot
    build: .
    env_file:
      - .env
    environment:
      - TZ=America/Los_Angeles
    volumes:
      - ./data:/app/data
    restart: unless-stopped
