services:
  discord-bot:
    container_name: discord-bot
    build: .
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - GUILD_ID=${GUILD_ID}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
