services:
  command-bot:
    container_name: command-bot
    build: .
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - GUILD_ID=${GUILD_ID}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
